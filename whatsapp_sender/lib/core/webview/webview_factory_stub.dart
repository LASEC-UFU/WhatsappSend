import 'app_webview_controller.dart';

/// Stub — nunca deve ser chamado diretamente.
/// Será substituído via conditional import.
AppWebViewController createWebViewController() {
  throw UnsupportedError(
    'Nenhuma implementação de WebView disponível nesta plataforma.',
  );
}
